# Python
__pycache__/
*.py[cod]
.venv/
*.egg-info/
.pytest_cache/
.ruff_cache/
uv.lock

# Node / Astro
node_modules/
site/dist/
site/.astro/

# Generated from data/events by `pipeline.run build` — never committed, so
# concurrent workflows can't collide on them. The deploy regenerates them.
site/src/data/events.json
out/

# Local env
.env
