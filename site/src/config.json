{
  "buttondownUsername": "shalakakulkarni",
  "_comment": "Buttondown account name. Switches on the email signup form across the site. If cleared, no form is rendered — an inert form that silently drops addresses is worse than none."
}
